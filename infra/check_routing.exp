#!/usr/bin/expect -f
set timeout 30
set ip "194.156.118.128"
set user "root"
set password "MaksimNiretin2602201213!"

spawn ssh -o StrictHostKeyChecking=no $user@$ip "curl -I -v -H 'Host: admin.td-rss.ru' http://127.0.0.1 && curl -I -v -H 'Host: api.td-rss.ru' http://127.0.0.1"

expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
