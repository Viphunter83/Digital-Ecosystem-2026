#!/usr/bin/expect -f
set timeout 30
set ip "194.156.118.128"
set user "root"
set password "MaksimNiretin2602201213!"

spawn ssh -o StrictHostKeyChecking=no $user@$ip "
echo '--- LS /ETC/DOKPLOY ---'
ls -R /etc/dokploy | grep -i traefik || echo 'No traefik found'
echo '--- LS ROOT ---'
ls /etc/dokploy
"

expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
