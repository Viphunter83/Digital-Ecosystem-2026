#!/usr/bin/expect -f
set timeout 20
set ip "194.156.118.128"
set user "root"
set password "MaksimNiretin2602201213!"

spawn ssh -o StrictHostKeyChecking=no $user@$ip "curl -I http://localhost:3000 && curl -I http://localhost:8000/docs && curl -I http://localhost:8055"

expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
