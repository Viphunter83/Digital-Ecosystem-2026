#!/usr/bin/expect -f
set timeout 20
set ip "194.156.118.128"
set user "root"
set password "MaksimNiretin2602201213!"

spawn ssh -o StrictHostKeyChecking=no $user@$ip "echo '--- ALL CONTAINERS ---'; docker ps -a; echo '--- SERVICES ---'; systemctl list-units --type=service --all | grep -i dokploy"

expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
