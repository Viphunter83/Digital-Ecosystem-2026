#!/usr/bin/expect -f
set timeout 300
set ip "194.156.118.128"
set user "root"
set password "MaksimNiretin2602201213!"

spawn ssh -o StrictHostKeyChecking=no $user@$ip "curl -fsSL https://get.docker.com -o get-docker.sh && sh get-docker.sh"

expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
