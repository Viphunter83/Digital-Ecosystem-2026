#!/usr/bin/expect -f
set timeout 30
set ip "194.156.118.128"
set user "root"
set password "MaksimNiretin2602201213!"

spawn ssh -o StrictHostKeyChecking=no $user@$ip "
echo '--- DIRECTUS NETWORKS ---'
docker inspect russtanko-russtankoprod-colyja-directus-1 --format '{{json .NetworkSettings.Networks}}'
echo '--- DIRECTUS FRESH LOGS (LAST 2m) ---'
docker logs --since 2m russtanko-russtankoprod-colyja-directus-1
"

expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
