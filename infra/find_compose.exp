#!/usr/bin/expect -f
set timeout 60
set ip "194.156.118.128"
set user "root"
set password "MaksimNiretin2602201213!"

spawn ssh -o StrictHostKeyChecking=no $user@$ip "find / -name 'docker-compose.yml' -not -path '*/overlay2/*' 2>/dev/null"

expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
