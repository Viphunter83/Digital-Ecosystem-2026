#!/usr/bin/expect -f
set timeout 30
set ip "194.156.118.128"
set user "root"
set password "MaksimNiretin2602201213!"

spawn ssh -o StrictHostKeyChecking=no $user@$ip "
echo '--- NETWORK MAP ---'
docker network inspect dokploy-network
echo '--- DIRECTUS ENV CHECK ---'
docker exec russtanko-russtankoprod-colyja-directus-1 env | grep DB_HOST
"

expect {
  "password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
