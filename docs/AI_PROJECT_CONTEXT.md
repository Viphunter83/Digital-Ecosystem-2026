# 🚀 Digital Ecosystem 2026 — AI Context Prompt

> **Дата последнего обновления**: 25 января 2026
> **Версия**: 1.6 (Актуализация: Корзина + Диагностика + TMA Flow)
> **Статус**: Стабилен, расширен функционал Mini App

---

## 📋 ОБЗОР И БИЗНЕС-ЛОГИКА
**Digital Ecosystem 2026** — B2B экосистема для ТД "РусСтанкоСбыт".
- **Каталог**: Гибридный поиск (Keyword + Semantic) промышленного оборудования.
- **Сервис**: Цифровые паспорта станков, отслеживание ТО, заявки инженерам через Telegram Mini App.
- **CRM**: Полная интеграция с AmoCRM (создание сделок из бота/сайта).

---

## 🔐 АКТУАЛЬНЫЕ НАСТРОЙКИ И ДОСТУПЫ

### 1. Directus 11 (CMS)
- **URL**: https://admin.td-rss.ru
- **Static Token**: `REDACTED_USE_TOKEN_FROM_DOKPLOY_ENV`
- **Документация**: См. [directus_user_guide.md](file:///Users/apple/Digital%20Ecosystem%202026/docs/directus_user_guide.md) для инструкций заказчику.
- **Медиа**: В коллекциях `products` и `spare_parts` используйте поле **Image File** (Directus Files) — это синхронизируется с ботом и сайтом.

### 2. Infrastructure (SSH & Dokploy)
- **Host**: `ssh root@<SERVER_IP>`
- **Path**: `/etc/dokploy/compose/russtanko-russtankoprod-colyja/code`
- **Restart**: Для обновления бота используйте `docker restart <CONTAINER_NAME>`.

---

## 🛠️ GIT И ПРОВЕРКА ПЕРЕД ПУШЕМ
**CRITICAL**: Всегда проверяй целостность кода перед пушем, так как Dokploy настроен на автодеплой из `main`.

**Checklist:**
1. **Frontend**: `npm run build --prefix apps/frontend` (проверяет типы, линтер и возможность сборки).
2. **Bot**: `python3 -m py_compile apps/bot/handlers.py` (проверка синтаксиса).
3. **Backend**: Проверяй импорты из `packages.database.models`.

**Git Workflow:**
```bash
git add .
git commit -m "feat: description"
git push origin main
```

---

## 🏗️ АРХИТЕКТУРА И СТРУКТУРА

```
Digital Ecosystem 2026/
├── apps/
│   ├── backend/          # FastAPI (Python + SQLAlchemy)
│   ├── frontend/         # Next.js 15 (TypeScript + Tailwind)
│   └── bot/              # Aiogram 3.x (Telegram Bot v1.1)
├── packages/
│   └── database/         # Общие модели и миграции (PostgreSQL + pgvector)
├── docs/                 # Инструкции (AmoCRM, Directus, Context)
├── .env                  # Конфигурация
└── docker-compose.prod.yml
```

---

## 🏗️ ТЕХНИЧЕСКИЕ ПАТТЕРНЫ (Guidelines)

### 1. Telegram Mini App (TMA)
- **Коммуникация**: Используй `tg.sendData(JSON.stringify({type: "TYPE", ...}))` для отправки данных обратно в бот.
- **Обработка в AIogram**: Все данные попадают в `web_app_data_handler` в `apps/bot/handlers.py`.
- **UX**: Обязательно используй `tg.HapticFeedback` и синхронизируй `BackButton` / `MainButton` с роутингом Next.js.

### 2. Состояние (Zustand + Persistence)
- **Hydration**: Для Next.js всегда используй паттерн с флагом `hydrated` в сторе (см. `useCartStore.ts`), чтобы избежать ошибок гидратации React.

### 3. Интеграция с Tilda (Backlog)
- Доступна через API `api.tildacdn.info`. Требуются `publickey` и `secretkey`. На текущий момент (январь 2026) идея на паузе, используется кастомный скрейпер.

---

## ✅ ЗАВЕРШЕНО (Январь 2026)
- [x] Стабилизация сессий Telegram бота.
- [x] Добавление выбора фото через библиотеку в CMS.
- [x] **Корзина**: Исправлена гидратация, добавлена навигация в TMA.
- [x] **Диагностика**: Реализована отправка AI-отчетов в бот ( rich formatting + haptics).
- [x] **Leads**: Интеграция результатов диагностики в AmoCRM через бэкенд.

---

## ⚠️ ЗАМЕТКИ ДЛЯ ИИ (Handover)
1. **Migrations**: Миграции в `packages/database/migrations` (формат `YYYYMMDDHHMMSS_name.sql`).
2. **Environment**: Новые переменные нужно добавлять И в `.env`, И в панель управления Dokploy.
3. **Media**: При загрузке файлов через API Directus всегда указывай `folder`.

---
*Документ полностью укомплектован для следующей сессии (v1.6).*
