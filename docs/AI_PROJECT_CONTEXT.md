# 🚀 Digital Ecosystem 2026 — AI Context Prompt

> **Дата последнего обновления**: 24 января 2026
> **Версия**: 1.5 (Полная актуализация: Стабилизация + Архитектура + Git)
> **Статус**: Стабилен, готов к расширению функционала

---

## 📋 ОБЗОР И БИЗНЕС-ЛОГИКА
**Digital Ecosystem 2026** — B2B экосистема для ТД "РусСтанкоСбыт".
- **Каталог**: Гибридный поиск (Keyword + Semantic) промышленного оборудования.
- **Сервис**: Цифровые паспорта станков, отслеживание ТО, заявки инженерам через Telegram Mini App.
- **CRM**: Полная интеграция с AmoCRM (создание сделок из бота/сайта).

---

## 🔐 АКТУАЛЬНЫЕ НАСТРОЙКИ И ДОСТУПЫ

### 1. Directus 11 (CMS)
- **URL**: https://admin.td-rss.ru
- **Static Token**: `REDACTED_USE_TOKEN_FROM_DOKPLOY_ENV`
- **UX**: В `spare_part_images` используйте поле **Image File** (кнопка выбора) вместо текстового `url`.
- **ИИ**: Используйте модуль **MCP** (настроен на `gpt-4o-mini`). Встроенный чат в сайдбаре временно нестабилен из-за GPT-5 протоколов.

### 2. Infrastructure (SSH & Dokploy)
- **Host**: `ssh root@<SERVER_IP>`
- **Path**: `/etc/dokploy/compose/russtanko-russtankoprod-colyja/code`
- **Команда рестарта бота**: `docker restart russtanko-russtankoprod-colyja-bot-1`

---

## 🛠️ GIT И РЕПОЗИТОРИЙ

- **URL**: `https://github.com/Viphunter83/Digital-Ecosystem-2026`
- **Ветка**: `main`

**Workflow деплоя:**
```bash
# 1. Локально
git add .
git commit -m "feat: description"
git push origin main

# 2. На сервере (автоматизация Dokploy подхватит, но если нужно вручную)
cd /etc/dokploy/compose/russtanko-russtankoprod-colyja/code
git pull origin main
docker-compose -f docker-compose.prod.yml up -d --build
```

---

## 🏗️ АРХИТЕКТУРА И СТРУКТУРА

```
Digital Ecosystem 2026/
├── apps/
│   ├── backend/          # FastAPI (Python + SQLAlchemy)
│   ├── frontend/         # Next.js 15 (TypeScript + Tailwind)
│   └── bot/              # Aiogram 3.x (Telegram Bot v1.1)
├── packages/
│   └── database/         # Общие модели и миграции (PostgreSQL + pgvector)
├── docs/                 # Инструкции (AmoCRM, Directus, Context)
├── .env                  # Конфигурация
└── docker-compose.prod.yml
```

**Связи данных:**
- Вся система использует **UUID** (идентичны в Directus и на сайте).
- **Frontend** общается с **Backend** через REST API.
- **Backend** синхронизирует данные из **Directus** в реальном времени.
1. **Directus API**: Если `read-collections` или другие MCP инструменты выдают `INVALID_CREDENTIALS`, проверь актуальность `DIRECTUS_TOKEN` (возьми из переменных окружения в Dokploy или из профиля админа).

---

## ✅ ЗАВЕРШЕНО (Январь 2026)
- [x] Стабилизация сессий Telegram бота.
- [x] Настройка `DIRECTUS_SECRET` для сохранения токенов.
- [x] Добавление выбора фото через библиотеку в CMS.
- [x] Обновление гайдов для заказчика.

---

## ⚠️ ЗАМЕТКИ ДЛЯ ИИ (Handover)
1. **Migrations**: При добавлении полей в БД всегда делай миграцию в `packages/database/migrations` (формат `YYYYMMDDHHMMSS_name.sql`).
2. **Environment**: Новые переменные нужно добавлять И в `.env`, И в панель управления Dokploy.
3. **Media**: При загрузке файлов через API всегда указывай `folder` ID (напр. "SpareParts"), чтобы они не падали в корень.
4. **ProxyAPI**: Базовый URL для OpenAI — `https://api.proxyapi.ru/openai/v1`.

---
*Документ полностью укомплектован для следующей сессии.*
