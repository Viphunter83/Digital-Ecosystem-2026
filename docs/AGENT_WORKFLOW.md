# AI-Agent Workflow & Project Synchronization

Этот документ описывает методологию работы AI-агента (Antigravity) в рамках проекта «Digital Ecosystem 2026». Данный подход гарантирует стабильность продакшн-среды и целостность кодовой базы.

## 🧠 Принципы работы

### 1. Тотальный контроль и синхронизация
Агент постоянно отслеживает состояние проекта в двух плоскостях:
- **Локальный репозиторий**: Ведется разработка, рефакторинг и подготовка изменений.
- **Продакшн-сервер (Remote)**: Через SSH-доступ агент проверяет переменные окружения, состояние Docker-контейнеров и реальные логи API.

Это исключает ситуацию «у меня работает, а на сервере нет», так как изменения верифицируются в реальных условиях.

### 2. Принцип «Thoughtful Changes» (Продуманные изменения)
Агент не вносит случайных правок. Каждый цикл изменений следует строгому алгоритму:
1. **Research**: Исследование текущего состояния кода и зависимостей.
2. **Planning**: Создание `implementation_plan.md` с описанием того, *что* и *зачем* будет изменено.
3. **Execution**: Точечное применение изменений с минимальным вмешательством в работающий код.
4. **Verification**: Проверка результата через эндпоинты, базу данных или автоматизированные браузерные тесты.

### 3. Синхронизация как стандарт
Любое изменение на сервере (например, исправление конфига в Dokploy) обязательно фиксируется в репозитории.
- **Git Push**: Выполняется только после того, как изменение проверено и признано стабильным.
- **Single Source of Truth**: Репозиторий GitHub всегда является мастер-копией проекта.

## 🛠 Инфраструктурный контроль
Агент обучен работе с:
- **Docker/Dokploy**: Управление жизненным циклом контейнеров.
- **FastAPI / SQLAlchemy**: Мониторинг здоровья бэкенда и целостности БД.
- **Integrations (AmoCRM, Telegram)**: Проверка связи на уровне сетевых запросов.

## ⚠️ Гарантии безопасности
Агент изолирует опасные инструменты (скрипты сброса БД и т.д.) в `infra/danger_zone` и никогда не запускает их автоматически без явного подтверждения риска, сохраняя при этом работоспособность всей экосистемы.
