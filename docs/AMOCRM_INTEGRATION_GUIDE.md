# Инструкция по интеграции AmoCRM: Digital Ecosystem 2026

Версия: 2.0 (Актуализировано: 23.01.2026)

## 📖 Обзор
Интеграция обеспечивает двусторонний обмен данными между платформой «РусСтанкоСбыт» и AmoCRM для автоматизации продаж и сервисного обслуживания.

### Основные потоки данных:
1. **Сайт/TMA ➔ AmoCRM**: Создание сделок (leads) при запросе сервиса или покупке запчастей.
2. **AmoCRM ➔ TMA**: Регистрация оборудования в «Цифровом Паспорте» клиента при успешном закрытии сделки.
3. **Двусторонняя синхронизация**: Обновление статусов обслуживания и дат ТО.

---

## � Чек-лист для Заказчика (Что нужно подготовить)

Для того чтобы мы могли завершить настройку интеграции, вам необходимо выполнить следующие действия на стороне вашего аккаунта AmoCRM:

### 1. Создать интеграцию
1. Зайдите в AmoCRM -> **Айти-решения** (Маркетплейс) -> **Разработчикам**.
2. Нажмите **+ Создать новую интеграцию**.
3. Выберите **«Внешняя интеграция»**.
4. В поле «Ссылка для перенаправления» укажите любой ваш сайт (например, `https://td-rss.ru`).
5. После создания перейдите во вкладку **«Ключи и токены»**.

### 2. Создать дополнительные поля в Сделках (Deals)
Вам нужно создать несколько полей для структурирования данных:
1. **Серийный номер** (Тип: Текст) — основной идентификатор станка.
2. **Модель оборудования** (Тип: Текст) — для запчастей тут может быть "Запчасти" или SKU.
3. **ID Telegram** (Тип: Текст) — для связи нашей системы с ботом клиента.

> [!TIP]
> **Для запчастей**: если клиент заказывает несколько позиций через корзину, система автоматически добавит **«Примечание»** к сделке со списком всех товаров, артикулов и цен. Это избавляет от необходимости создавать десятки полей под каждую гайку.

### 3. Найти ID воронки и менеджера
1. Откройте вашу воронку продаж. В адресной строке вы увидите число после `/leads/pipeline/` — это **Pipeline ID**.
2. Перейдите в настройки пользователей и кликните на менеджера, который будет отвечать за заявки — в ссылке будет его **User ID**.

---

## 📋 Какие данные передать разработчикам

Пожалуйста, соберите и отправьте нам следующие данные:

1. **Поддомен**: (например, `russtanko`).
2. **Access Token**: (Длинный код из раздела «Ключи и токены» -> «Долгосрочный токен»).
3. **Pipeline ID**: ID воронки, куда должны падать заявки.
4. **Responsible User ID**: ID менеджера по умолчанию.
5. **IDs созданных полей**:
   - ID поля «Серийный номер»
   - ID поля «Тип оборудования»
   - ID поля «ID Telegram»
   *(Чтобы узнать ID поля: в настройках полей AmoCRM кликните на название поля, ID отобразится в углу окна).*

---


## �🛠 Настройка Интеграции

### 1. Переменные окружения (Environment)
Для работы интеграции в `apps/backend/.env` должны быть настроены следующие параметры:
```env
AMOCRM_SUBDOMAIN=russtanko               # Ваш поддомен в AmoCRM
AMOCRM_ACCESS_TOKEN=xxx                  # Long-lived Access Token
AMOCRM_PIPELINE_ID=1234567               # ID воронки продаж
AMOCRM_RESPONSIBLE_USER_ID=987654        # ID ответственного менеджера
AMOCRM_STATUS_SUCCESS=142                # ID статуса "Успешно реализовано"
AMOCRM_FIELD_SERIAL_ID=111111            # ID поля "Серийный номер"
AMOCRM_FIELD_MODEL_ID=222222             # ID поля "Модель/Slug"
AMOCRM_FIELD_TELEGRAM_ID=333333          # ID поля "ID Telegram"
```

### 2. Настройка Custom Fields (Дополнительные поля)
В AmoCRM необходимо создать следующие поля в Сделках (Deals) и записать их ID:
- **Серийный номер**: Текст (привязка к `MachineInstance.serial_number`)
- **Конфигурация**: Текст/JSON (привязка к спецификации оборудования)
- **Дата следующего ТО**: Дата (привязка к `next_maintenance_date`)

### 3. Настройка Webhooks (Должен сделать тот, у кого есть доступ в AmoCRM)
В настройках AmoCRM (Разработчикам -> Вебхуки) добавьте URL:
`https://api.td-rss.ru/webhook/amocrm`

**Кто это делает:** Обычно заказчик под вашим руководством или вы сами, если у вас есть доступ администратора к его аккаунту AmoCRM.

События для подписки:
- [x] Сделка изменена (статус переведен в «Успешно реализовано»)
- [x] Контакт изменен

---

## 🔄 Логика Двустороннего Обмена

### А. Регистрация нового оборудования
Когда менеджер переводит сделку в этап **«Успешно реализовано»** (ID статуса: `142`):
1. AmoCRM отправляет вебхук в систему на `/webhook/amocrm`.
2. Система извлекает данные из полей:
   - `CF_SERIAL` (Серийный номер)
   - `CF_MODEL` (Slug модели в каталоге)
3. Система находит клиента и создает запись в таблице `machine_instances`.
4. Клиент получает уведомление в Telegram: *"Ваше оборудование зарегистрировано!"*.
5. В TMA в разделе «Мой Парк» появляется новый цифровой паспорт.

### Б. Сервисные уведомления
1. Планировщик на стороне Бэкенда (`scheduler.py`) проверяет даты ТО.
2. Если до ТО осталось 30 дней, система отправляет уведомление клиенту.
3. **Важно**: Система может также создать задачу или сделку в AmoCRM для менеджера, чтобы он предложил расходники («Допродажа»).

### В. Обратная связь по сервису
1. При создании заявки через TMA в AmoCRM создается Сделка.
2. Если инженер на сайте отмечает работу как «Выполнено», статус сделки в AmoCRM автоматически обновляется (Push-уведомление от Бэкенда).

---

## 🔍 Диагностика и Отладка
- **Логи Бэкенда**: `docker logs russtanko-backend | grep AmoCRM`
- **Тестовый вебхук**:
  ```bash
  curl -X POST https://api.td-rss.ru/webhook/amocrm \
       -H "Content-Type: application/x-www-form-urlencoded" \
       -d "leads[status][0][id]=12345&leads[status][0][status_id]=142"
  ```

---

## ⚠️ Рекомендации по безопасности
- Всегда используйте Long-lived токены AmoCRM.
- Проверяйте IP-адреса входящих вебхуков (AmoCRM использует фиксированные рейнджи).
- Не храните Client Secret в клиентском коде фронтенда.

**Нужна помощь?** Если у вас возникли сложности с поиском ID или созданием полей, мы можем провести настройку совместно через Zoom/AnyDesk.
