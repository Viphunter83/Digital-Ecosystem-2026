# Handover: Session 10.02.2026 - Website Recovery & Content Logic

Этот документ фиксирует итоги сессии по восстановлению сайта и исправлению логики отображения контента.

## 🚀 Текущий статус: Система восстановлена ✅
После критического сбоя при деплое, все сервисы возвращены в рабочее состояние.

### 1. Инфраструктура (Docker & Dokploy)
*   **Конфликт портов**: Исправлен конфликт порта 3000. В `docker-compose.prod.yml` удален проброс порта на хост, теперь Traefik роутит трафик напрямую в контейнер.
*   **Данные (Volumes)**: Восстановлена привязка к внешним томам БД (`db_data`) и Directus (`uploads`). Это вернуло каталог товаров (18 позиций).
*   **Синхронизация**: Локальные изменения в `docker-compose.prod.yml` и `.env` синхронизированы с сервером.

### 2. FAQ и API Logic
*   **Проблема**: Раздел FAQ был пуст, так как клиентский код (Next.js) пытался достучаться до `http://backend:8000` — внутреннего адреса Docker, недоступного из браузера.
*   **Исправление**: 
    *   В `lib/api.ts` добавлена гибридная логика: в браузере используется публичный эндпоинт `https://api.td-rss.ru`, на сервере — внутренний `http://backend:8000`.
    *   `NEXT_PUBLIC_API_URL` обновлен до `https://api.td-rss.ru`.
*   **Результат**: FAQ отображается и работает интерактивно.

### 3. AmoCRM
*   **Стабильность**: Интеграция переведена на долгосрочный токен (действителен до **2031 года**). Это устраняет риск блокировки лидов из-за просроченной авторизации.
*   **Проверка**: Тестовые лиды успешно создаются и попадают в воронку «Клиенты ООО».

## 📋 План на следующую сессию (Final Testing)
- **Финальный прогон**: Тестирование всех форм обратной связи на сайте.
- **Мобильная версия**: Проверка работы TMA (Telegram Mini App) через бота @Russtanko2026_bot.
- **Email**: Подтверждение получения уведомлений на `zakaz@tdrusstankosbyt.ru`.
- **UX**: Проверка плавности анимаций и корректности отображения карт.

---
*Документация актуализирована в /docs/ARCHITECTURE.md и /docs/DEPLOYMENT.md*
